{
	"name": "personalwebsite",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"predeploy": "npm run build",
		"deploy": "echo ncp.dev > ./dist/CNAME && gh-pages -d dist",
		"dev": "vite",
		"prebuild": "",
		"build": "vite build",
		"postbuild": "ncp dist/src/404-page dist && ncp dist/src dist/ && del-cli --force dist/src && del-cli --force dist/404-page",
		"preview": "vite preview"
	},
	"devDependencies": {
		"del-cli": "^5.0.0",
		"gh-pages": "^4.0.0",
		"vite": "^3.2.5"
	},
	"dependencies": {
		"ncp": "^2.0.0",
		"node": "^19.0.0"
	}
}
